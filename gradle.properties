# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 0.1.5
maven_group = net.ramixin
archives_base_name = mixson

# Dependencies are managed at gradle/libs.versions.toml
